# RevealLine Archive30: original v0.62.0 and v0.62.1

This append candidate preserves both original published distributions. Accepted predecessor infrastructure `cf9113746d96cf50d7cb8491d58dece53bd9cc60`, tree `c3baca75f63c6e886b6cfa3a0f7a81f3da0d4188`, retains the original v0.62.0 cohort. The new v0.62.1 cohort uses source `9f04273a09dd23d44236c1715d62cfa460171e24`, tree `2c35f299f981aba98929ba9ac01addbfe8b48d63`, annotated tag `23d74a6b7df7e34154ee916736fa463a63748bc8` and retained Release `391933235`. Fresh remote authority, merged successor infrastructure, hosted extraction/deployment, complete public byte verification and native archive admission remain pending.

Expected complete inventory: **1,408 rows / 627,086,405 bytes**, below the unchanged 800,000,000-byte budget by 172,913,595 bytes. It retains all 705 prior paths: 704 rows byte-identical, including all 702 original v0.62.0 release rows and the unchanged release-explorer bridge/hidden marker. Only mutable root `index.html` changes to list both versions (+79 bytes). The appended cohort contains 703 rows / 313,522,906 bytes. No immutable body, source, tag, release metadata or qualification is rewritten.

`source-lock.json` binds both original ZIPs, exact original metadata/qualification and the unchanged reviewed extractor. The workflow explicitly fetches both original tags, verifies annotated/peeled identities, extracts original ZIPs and independently hashes every file, including hidden files, before rechecking authorities. Its 3 GiB hosted guard, 20,000-row limit, main-only deployment and non-cancelling concurrency remain. Production `tools/prepare.py` and `tools/verify.py` are byte-identical to the accepted predecessor. Only existing committed-metadata fixture expectations change for the appended cohort.

`input-authority.json` preserves the predecessor's historical preparation record unchanged. `append-input-authority.json` records the retained original v0.62.1 metadata and scoped acceptance sources; it does not assign future run/deployment IDs. Archive acceptance must retain real extraction receipts, complete successor HTTP inventory and actual keyboard entry/play/Pause/Resume/focus/Release explorer return. Carry forward existing offline, physical-input and v0.62.0 Team limitations; this append does not repair a frozen edition or establish new gameplay acceptance.

Use the archive workflow for original-ZIP extraction; do not rebuild historical source or download source.tar. Main remains selected at v0.62.1 until this archive successor and the next current release are separately accepted. No new archive repository is required.
