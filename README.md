# RevealLine Archive30: original v0.62.0

This candidate preserves the exact released v0.62.0 distribution. Repository creation, hosted extraction/deployment, complete archive HTTP verification and archive-native admission remain pending. The proposed archive URL is not a verified deployment.

Frozen source `72b6845e101850a4ee34df66656dae3c7ad87f5f`, tree `3fda06b8a0b2efd6f500852bde42d6745b87df42`, annotated tag `8994ae79181904bd1ddc588441dc0b8a0432dff0`, release 391889344. The input authority pins nine original release assets, four original small metadata bodies and retained public acceptance. The source lock binds the original ZIP, qualification and reviewed extractor. No historical rebuild or source.tar is used.

Expected inventory: **705 rows / 313,563,420 bytes**, comprising 702 release rows / 313,562,026 bytes plus the root index, release-explorer redirect and hidden .nojekyll marker. Zero prior accepted/preserved rows and zero changed prior paths. Remaining capacity: 486,436,580 bytes under the unchanged 800,000,000-byte budget. Existing archive29 remains untouched; adding this cohort there would exceed its budget.

The workflow explicitly fetches the sole locked v0.62.0 tag, verifies annotated/peeled identities, extracts the original ZIP and independently verifies every byte, then rechecks source authorities. The 3 GiB hosted guard, 20,000-row limit, main-only deployment and noncancelling concurrency remain. Preparation never downloads the original payload or runs full extraction locally.

Main v0.62.0 is separately accepted within its scoped limits: 3,311 public files / 643,489,071 bytes, 3,313 attempts with two recovered timeouts and zero final failures/skips. Two source families each passed 6,094 tests across 476 files on actual hosted Node20.19.6. These historical source/main facts do not qualify this archive. Public Solo/Versus wins and continuation were observed; Team win/Next, physical controllers/touch, offline/audio and full cross-mode acceptance remain open. Missions v0.62.1 is a separate candidate and does not amend this immutable edition.

Root must retain actual successful archive run/deployment/small receipts, independently reconcile every public row with fresh unchanged authorities, and perform scoped keyboard entry/play/Pause/Resume/focus/release-explorer return before admission. Preserve save ownership and disclose session-only behavior; shared origin alone proves no storage isolation. Workflow success is insufficient.
